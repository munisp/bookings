[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# These tests drive a real compose stack; individual waits can be long.
addopts = -ra --tb=short
markers =
    docker: requires a live docker host with the OpenDesk compose stack
