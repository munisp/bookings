[package]
name = "pii-redact"
group = "opendesk"
version = "0.1.0"
apiVersion = "0.1.0"
description = "Redact phone numbers (E.164-ish) and emails from transcript records before sinking to OpenSearch/lakehouse"
license = "Apache-2.0"

[params]
